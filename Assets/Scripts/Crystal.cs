using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DG.Tweening;

public class Crystal :Collide
{
  // private Sequence _upAndDawn;

    private void Start()
    {
       /* _upAndDawn = DOTween.Sequence();
        _upAndDawn.Append(transform.DOMoveY(1f, 0.5f)).Append(transform.DOShakeScale(2f,0.3f))
            .Append(transform.DOMoveY(0.3f, 0.5f))
            .SetLoops(100,LoopType.Restart).Play();*/
    }
}
